#!/bin/sh
# Перед стартом применяем миграции БД (если база доступна).
set -e
if [ -n "$DATABASE_URL" ]; then
  echo "Применяю миграции базы данных…"
  npx prisma migrate deploy || echo "Миграции не применены — приложение продолжит работу без БД"
fi
exec "$@"
